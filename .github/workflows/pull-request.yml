name: Pull Request

on:
  push:
    branches:
      - '*'

jobs:
  build-and-test:
    uses: ./.github/workflows/base-ci.yml
    permissions:
      contents: write
      issues: write
      checks: write
      pull-requests: write
